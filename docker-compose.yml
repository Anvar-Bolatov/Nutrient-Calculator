
services:
  backend:
    build: .
    ports:
      - 8000:8000
    env_file:
      - .env
    image: yourbestfriend8901/nutrient-calc:latest
    volumes:
      - ./logs:/Nutrient-Calculator/logs
     